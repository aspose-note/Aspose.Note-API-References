---
title: "NodeType"
second_title: "Aspose.Note for Java API 参考"
description: "指定节点的类型。"
type: docs
weight: 52
url: /zh/java/com.aspose.note/nodetype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NodeType extends System.Enum
```

指定节点的类型。
## 字段

| 字段 | 描述 |
| --- | --- |
| [AttachedFile](#AttachedFile) | 指定该节点是一个 `AttachedFile`。 |
| [Document](#Document) | 指定该节点是一个 `Document`。 |
| [Image](#Image) | 指定该节点是一个 `Image`。 |
| [InkDrawing](#InkDrawing) | 指定该节点是一个 [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing)。 |
| [InkParagraph](#InkParagraph) | 指定该节点是一个 [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph)。 |
| [InkWord](#InkWord) | 指定该节点是一个 [InkWord](../../com.aspose.note/nodetype\#InkWord)。 |
| [Loop](#Loop) | 指定该节点是一个 [Loop](../../com.aspose.note/nodetype\#Loop)。 |
| [Outline](#Outline) | 指定该节点是一个 `Outline`。 |
| [OutlineElement](#OutlineElement) | 指定该节点是一个 `OutlineElement`。 |
| [OutlineGroup](#OutlineGroup) | 指定该节点是一个 `OutlineGroup`。 |
| [Page](#Page) | 指定该节点是一个 `Page`。 |
| [PageSeries](#PageSeries) | 指定该节点是一个 `PageSeries`。 |
| [RichText](#RichText) | 指定该节点是一个 `RichText`。 |
| [Section](#Section) | 指定该节点是一个 `Section`。 |
| [Table](#Table) | 指定该节点是一个 `Table`。 |
| [TableCell](#TableCell) | 指定该节点是一个 `TableCell`。 |
| [TableRow](#TableRow) | 指定该节点是一个 `TableRow`。 |
| [Title](#Title) | 指定该节点是一个 `Title`。 |
### AttachedFile {#AttachedFile}
```
public static final int AttachedFile
```


指定该节点是一个 `AttachedFile`。

### Document {#Document}
```
public static final int Document
```


指定该节点是一个 `Document`。

### Image {#Image}
```
public static final int Image
```


指定该节点是一个 `Image`。

### InkDrawing {#InkDrawing}
```
public static final int InkDrawing
```


指定该节点是一个 [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing)。

### InkParagraph {#InkParagraph}
```
public static final int InkParagraph
```


指定该节点是一个 [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph)。

### InkWord {#InkWord}
```
public static final int InkWord
```


指定该节点是一个 [InkWord](../../com.aspose.note/nodetype\#InkWord)。

### Loop {#Loop}
```
public static final int Loop
```


指定该节点是一个 [Loop](../../com.aspose.note/nodetype\#Loop)。

### Outline {#Outline}
```
public static final int Outline
```


指定该节点是一个 `Outline`。

### OutlineElement {#OutlineElement}
```
public static final int OutlineElement
```


指定该节点是一个 `OutlineElement`。

### OutlineGroup {#OutlineGroup}
```
public static final int OutlineGroup
```


指定该节点是一个 `OutlineGroup`。

### Page {#Page}
```
public static final int Page
```


指定该节点是一个 `Page`。

### PageSeries {#PageSeries}
```
public static final int PageSeries
```


指定该节点是一个 `PageSeries`。

### RichText {#RichText}
```
public static final int RichText
```


指定该节点是一个 `RichText`。

### Section {#Section}
```
public static final int Section
```


指定该节点是一个 `Section`。

### Table {#Table}
```
public static final int Table
```


指定该节点是一个 `Table`。

### TableCell {#TableCell}
```
public static final int TableCell
```


指定该节点是一个 `TableCell`。

### TableRow {#TableRow}
```
public static final int TableRow
```


指定该节点是一个 `TableRow`。

### Title {#Title}
```
public static final int Title
```


指定该节点是一个 `Title`。

