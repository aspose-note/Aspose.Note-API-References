---
title: "NodeType"
second_title: "Riferimento API di Aspose.Note per Java"
description: "Specifica il tipo del nodo."
type: docs
weight: 52
url: /it/java/com.aspose.note/nodetype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NodeType extends System.Enum
```

Specifica il tipo del nodo.
## Campi

| Campo | Descrizione |
| --- | --- |
| [AttachedFile](#AttachedFile) | Specifica che il nodo è un `AttachedFile`. |
| [Document](#Document) | Specifica che il nodo è un `Document`. |
| [Image](#Image) | Specifica che il nodo è un'`Image`. |
| [InkDrawing](#InkDrawing) | Specifica che il nodo è un [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing). |
| [InkParagraph](#InkParagraph) | Specifica che il nodo è un [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph). |
| [InkWord](#InkWord) | Specifica che il nodo è un [InkWord](../../com.aspose.note/nodetype\#InkWord). |
| [Loop](#Loop) | Specifica che il nodo è un [Loop](../../com.aspose.note/nodetype\#Loop). |
| [Outline](#Outline) | Specifica che il nodo è un'`Outline`. |
| [OutlineElement](#OutlineElement) | Specifica che il nodo è un'`OutlineElement`. |
| [OutlineGroup](#OutlineGroup) | Specifica che il nodo è un'`OutlineGroup`. |
| [Page](#Page) | Specifica che il nodo è una `Page`. |
| [PageSeries](#PageSeries) | Specifica che il nodo è una `PageSeries`. |
| [RichText](#RichText) | Specifica che il nodo è un `RichText`. |
| [Section](#Section) | Specifica che il nodo è una `Section`. |
| [Table](#Table) | Specifica che il nodo è una `Table`. |
| [TableCell](#TableCell) | Specifica che il nodo è una `TableCell`. |
| [TableRow](#TableRow) | Specifica che il nodo è una `TableRow`. |
| [Title](#Title) | Specifica che il nodo è un `Title`. |
### AttachedFile {#AttachedFile}
```
public static final int AttachedFile
```


Specifica che il nodo è un `AttachedFile`.

### Document {#Document}
```
public static final int Document
```


Specifica che il nodo è un `Document`.

### Image {#Image}
```
public static final int Image
```


Specifica che il nodo è un'`Image`.

### InkDrawing {#InkDrawing}
```
public static final int InkDrawing
```


Specifica che il nodo è un [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing).

### InkParagraph {#InkParagraph}
```
public static final int InkParagraph
```


Specifica che il nodo è un [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph).

### InkWord {#InkWord}
```
public static final int InkWord
```


Specifica che il nodo è un [InkWord](../../com.aspose.note/nodetype\#InkWord).

### Loop {#Loop}
```
public static final int Loop
```


Specifica che il nodo è un [Loop](../../com.aspose.note/nodetype\#Loop).

### Outline {#Outline}
```
public static final int Outline
```


Specifica che il nodo è un'`Outline`.

### OutlineElement {#OutlineElement}
```
public static final int OutlineElement
```


Specifica che il nodo è un'`OutlineElement`.

### OutlineGroup {#OutlineGroup}
```
public static final int OutlineGroup
```


Specifica che il nodo è un'`OutlineGroup`.

### Page {#Page}
```
public static final int Page
```


Specifica che il nodo è una `Page`.

### PageSeries {#PageSeries}
```
public static final int PageSeries
```


Specifica che il nodo è una `PageSeries`.

### RichText {#RichText}
```
public static final int RichText
```


Specifica che il nodo è un `RichText`.

### Section {#Section}
```
public static final int Section
```


Specifica che il nodo è una `Section`.

### Table {#Table}
```
public static final int Table
```


Specifica che il nodo è una `Table`.

### TableCell {#TableCell}
```
public static final int TableCell
```


Specifica che il nodo è una `TableCell`.

### TableRow {#TableRow}
```
public static final int TableRow
```


Specifica che il nodo è una `TableRow`.

### Title {#Title}
```
public static final int Title
```


Specifica che il nodo è un `Title`.

