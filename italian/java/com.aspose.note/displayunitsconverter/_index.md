---
title: "DisplayUnitsConverter"
second_title: "Riferimento API di Aspose.Note per Java"
description: "La classe contiene i metodi per convertire i valori."
type: docs
weight: 19
url: /it/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

La classe contiene i metodi per convertire i valori.
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Converte i pollici in punti. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Converte i millimetri in pollici. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Converte i millimetri in punti. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Converte i pixel in punti alla risoluzione pixel specificata. |
| [pointToInch(float points)](#pointToInch-float-) | Converte i punti in pollici. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Converte i punti in pixel alla risoluzione pixel specificata. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Converte i pollici in punti.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pollici | float | Il valore da convertire in pollici. |

**Returns:**
float - Il `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Converte i millimetri in pollici.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| mm | float | Il valore da convertire in millimetri. |

**Returns:**
float - Il `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Converte i millimetri in punti.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| mm | float | Il valore da convertire in millimetri. |

**Returns:**
float - Il `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Converte i pixel in punti alla risoluzione pixel specificata.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| pixel | int | Il valore da convertire in pixel. |
| dpi | float | Risoluzione dello schermo. |

**Returns:**
float - L'`int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Converte i punti in pollici.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| punti | float | Il valore da convertire in punti. |

**Returns:**
float - Il `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Converte i punti in pixel alla risoluzione pixel specificata.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| punti | float | Il valore da convertire in punti. |
| dpi | float | Risoluzione dello schermo. |

**Returns:**
int - Il `int`.
