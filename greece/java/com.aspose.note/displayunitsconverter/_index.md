---
title: "DisplayUnitsConverter"
second_title: "Aspose.Note για Java Αναφορά API"
description: "Η κλάση περιέχει τις μεθόδους για τη μετατροπή τιμών."
type: docs
weight: 19
url: /el/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

Η κλάση περιέχει τις μεθόδους για τη μετατροπή τιμών.
## Μέθοδοι

| Μέθοδος | Περιγραφή |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Μετατρέπει ίντσες σε πόντους. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Μετατρέπει χιλιοστά σε ίντσες. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Μετατρέπει χιλιοστά σε πόντους. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Μετατρέπει εικονοστοιχεία σε πόντους με την καθορισμένη ανάλυση εικονοστοιχείων. |
| [pointToInch(float points)](#pointToInch-float-) | Μετατρέπει πόντους σε ίντσες. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Μετατρέπει πόντους σε εικονοστοιχεία με την καθορισμένη ανάλυση εικονοστοιχείων. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Μετατρέπει ίντσες σε πόντους.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| ίντσες | float | Η τιμή προς μετατροπή σε ίντσες. |

**Returns:**
float - Το `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Μετατρέπει χιλιοστά σε ίντσες.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| mm | float | Η τιμή προς μετατροπή σε χιλιοστά. |

**Returns:**
float - Το `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Μετατρέπει χιλιοστά σε πόντους.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| mm | float | Η τιμή προς μετατροπή σε χιλιοστά. |

**Returns:**
float - Το `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Μετατρέπει εικονοστοιχεία σε πόντους με την καθορισμένη ανάλυση εικονοστοιχείων.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| εικονοστοιχεία | int | Η τιμή προς μετατροπή σε εικονοστοιχεία. |
| dpi | float | Ανάλυση οθόνης. |

**Returns:**
float - Το `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Μετατρέπει πόντους σε ίντσες.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| πόντοι | float | Η τιμή προς μετατροπή σε πόντους. |

**Returns:**
float - Το `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Μετατρέπει πόντους σε εικονοστοιχεία με την καθορισμένη ανάλυση εικονοστοιχείων.

**Parameters:**
| Παράμετρος | Τύπος | Περιγραφή |
| --- | --- | --- |
| πόντοι | float | Η τιμή προς μετατροπή σε πόντους. |
| dpi | float | Ανάλυση οθόνης. |

**Returns:**
int - Το `int`.
