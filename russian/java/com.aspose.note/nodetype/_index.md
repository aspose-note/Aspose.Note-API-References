---
title: "NodeType"
second_title: "Справочник API Aspose.Note for Java"
description: "Указывает тип узла."
type: docs
weight: 52
url: /ru/java/com.aspose.note/nodetype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NodeType extends System.Enum
```

Указывает тип узла.
## Поля

| Поле | Описание |
| --- | --- |
| [AttachedFile](#AttachedFile) | Указывает, что узел является `AttachedFile`. |
| [Document](#Document) | Указывает, что узел является `Document`. |
| [Image](#Image) | Указывает, что узел является `Image`. |
| [InkDrawing](#InkDrawing) | Указывает, что узел является [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing). |
| [InkParagraph](#InkParagraph) | Указывает, что узел является [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph). |
| [InkWord](#InkWord) | Указывает, что узел является [InkWord](../../com.aspose.note/nodetype\#InkWord). |
| [Loop](#Loop) | Указывает, что узел является [Loop](../../com.aspose.note/nodetype\#Loop). |
| [Outline](#Outline) | Указывает, что узел является `Outline`. |
| [OutlineElement](#OutlineElement) | Указывает, что узел является `OutlineElement`. |
| [OutlineGroup](#OutlineGroup) | Указывает, что узел является `OutlineGroup`. |
| [Page](#Page) | Указывает, что узел является `Page`. |
| [PageSeries](#PageSeries) | Указывает, что узел является `PageSeries`. |
| [RichText](#RichText) | Указывает, что узел является `RichText`. |
| [Section](#Section) | Указывает, что узел является `Section`. |
| [Table](#Table) | Указывает, что узел является `Table`. |
| [TableCell](#TableCell) | Указывает, что узел является `TableCell`. |
| [TableRow](#TableRow) | Указывает, что узел является `TableRow`. |
| [Title](#Title) | Указывает, что узел является `Title`. |
### AttachedFile {#AttachedFile}
```
public static final int AttachedFile
```


Указывает, что узел является `AttachedFile`.

### Document {#Document}
```
public static final int Document
```


Указывает, что узел является `Document`.

### Image {#Image}
```
public static final int Image
```


Указывает, что узел является `Image`.

### InkDrawing {#InkDrawing}
```
public static final int InkDrawing
```


Указывает, что узел является [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing).

### InkParagraph {#InkParagraph}
```
public static final int InkParagraph
```


Указывает, что узел является [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph).

### InkWord {#InkWord}
```
public static final int InkWord
```


Указывает, что узел является [InkWord](../../com.aspose.note/nodetype\#InkWord).

### Loop {#Loop}
```
public static final int Loop
```


Указывает, что узел является [Loop](../../com.aspose.note/nodetype\#Loop).

### Outline {#Outline}
```
public static final int Outline
```


Указывает, что узел является `Outline`.

### OutlineElement {#OutlineElement}
```
public static final int OutlineElement
```


Указывает, что узел является `OutlineElement`.

### OutlineGroup {#OutlineGroup}
```
public static final int OutlineGroup
```


Указывает, что узел является `OutlineGroup`.

### Page {#Page}
```
public static final int Page
```


Указывает, что узел является `Page`.

### PageSeries {#PageSeries}
```
public static final int PageSeries
```


Указывает, что узел является `PageSeries`.

### RichText {#RichText}
```
public static final int RichText
```


Указывает, что узел является `RichText`.

### Section {#Section}
```
public static final int Section
```


Указывает, что узел является `Section`.

### Table {#Table}
```
public static final int Table
```


Указывает, что узел является `Table`.

### TableCell {#TableCell}
```
public static final int TableCell
```


Указывает, что узел является `TableCell`.

### TableRow {#TableRow}
```
public static final int TableRow
```


Указывает, что узел является `TableRow`.

### Title {#Title}
```
public static final int Title
```


Указывает, что узел является `Title`.

