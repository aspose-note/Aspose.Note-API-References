---
title: "DisplayUnitsConverter"
second_title: "Справочник API Aspose.Note for Java"
description: "Класс содержит методы для преобразования значений."
type: docs
weight: 19
url: /ru/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

Класс содержит методы для преобразования значений.
## Методы

| Метод | Описание |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Преобразует дюймы в пункты. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Преобразует миллиметры в дюймы. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Преобразует миллиметры в пункты. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Преобразует пиксели в пункты при указанном разрешении пикселей. |
| [pointToInch(float points)](#pointToInch-float-) | Преобразует пункты в дюймы. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Преобразует пункты в пиксели при указанном разрешении пикселей. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Преобразует дюймы в пункты.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| дюймы | float | Значение для преобразования в дюймах. |

**Returns:**
float - Это `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Преобразует миллиметры в дюймы.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| мм | float | Значение для преобразования в миллиметрах. |

**Returns:**
float - Это `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Преобразует миллиметры в пункты.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| мм | float | Значение для преобразования в миллиметрах. |

**Returns:**
float - Это `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Преобразует пиксели в пункты при указанном разрешении пикселей.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| пиксели | int | Значение для преобразования в пикселях. |
| dpi | float | Разрешение экрана. |

**Returns:**
float - Это `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Преобразует пункты в дюймы.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| пункты | float | Значение для преобразования в пунктах. |

**Returns:**
float - Это `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Преобразует пункты в пиксели при указанном разрешении пикселей.

**Parameters:**
| Параметр | Тип | Описание |
| --- | --- | --- |
| пункты | float | Значение для преобразования в пунктах. |
| dpi | float | Разрешение экрана. |

**Returns:**
int - Тип `int`.
