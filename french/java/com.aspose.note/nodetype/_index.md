---
title: "NodeType"
second_title: "Référence d'API Aspose.Note pour Java"
description: "Spécifie le type du nœud."
type: docs
weight: 52
url: /fr/java/com.aspose.note/nodetype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NodeType extends System.Enum
```

Spécifie le type du nœud.
## Champs

| Champ | Description |
| --- | --- |
| [AttachedFile](#AttachedFile) | Spécifie que le nœud est un `AttachedFile`. |
| [Document](#Document) | Spécifie que le nœud est un `Document`. |
| [Image](#Image) | Spécifie que le nœud est une `Image`. |
| [InkDrawing](#InkDrawing) | Spécifie que le nœud est un [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing). |
| [InkParagraph](#InkParagraph) | Spécifie que le nœud est un [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph). |
| [InkWord](#InkWord) | Spécifie que le nœud est un [InkWord](../../com.aspose.note/nodetype\#InkWord). |
| [Loop](#Loop) | Spécifie que le nœud est une [Loop](../../com.aspose.note/nodetype\#Loop). |
| [Outline](#Outline) | Spécifie que le nœud est un `Outline`. |
| [OutlineElement](#OutlineElement) | Spécifie que le nœud est un `OutlineElement`. |
| [OutlineGroup](#OutlineGroup) | Spécifie que le nœud est un `OutlineGroup`. |
| [Page](#Page) | Spécifie que le nœud est une `Page`. |
| [PageSeries](#PageSeries) | Spécifie que le nœud est une `PageSeries`. |
| [RichText](#RichText) | Spécifie que le nœud est un `RichText`. |
| [Section](#Section) | Spécifie que le nœud est une `Section`. |
| [Table](#Table) | Spécifie que le nœud est une `Table`. |
| [TableCell](#TableCell) | Spécifie que le nœud est une `TableCell`. |
| [TableRow](#TableRow) | Spécifie que le nœud est une `TableRow`. |
| [Title](#Title) | Spécifie que le nœud est un `Title`. |
### AttachedFile {#AttachedFile}
```
public static final int AttachedFile
```


Spécifie que le nœud est un `AttachedFile`.

### Document {#Document}
```
public static final int Document
```


Spécifie que le nœud est un `Document`.

### Image {#Image}
```
public static final int Image
```


Spécifie que le nœud est une `Image`.

### InkDrawing {#InkDrawing}
```
public static final int InkDrawing
```


Spécifie que le nœud est un [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing).

### InkParagraph {#InkParagraph}
```
public static final int InkParagraph
```


Spécifie que le nœud est un [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph).

### InkWord {#InkWord}
```
public static final int InkWord
```


Spécifie que le nœud est un [InkWord](../../com.aspose.note/nodetype\#InkWord).

### Loop {#Loop}
```
public static final int Loop
```


Spécifie que le nœud est une [Loop](../../com.aspose.note/nodetype\#Loop).

### Outline {#Outline}
```
public static final int Outline
```


Spécifie que le nœud est un `Outline`.

### OutlineElement {#OutlineElement}
```
public static final int OutlineElement
```


Spécifie que le nœud est un `OutlineElement`.

### OutlineGroup {#OutlineGroup}
```
public static final int OutlineGroup
```


Spécifie que le nœud est un `OutlineGroup`.

### Page {#Page}
```
public static final int Page
```


Spécifie que le nœud est une `Page`.

### PageSeries {#PageSeries}
```
public static final int PageSeries
```


Spécifie que le nœud est une `PageSeries`.

### RichText {#RichText}
```
public static final int RichText
```


Spécifie que le nœud est un `RichText`.

### Section {#Section}
```
public static final int Section
```


Spécifie que le nœud est une `Section`.

### Table {#Table}
```
public static final int Table
```


Spécifie que le nœud est une `Table`.

### TableCell {#TableCell}
```
public static final int TableCell
```


Spécifie que le nœud est une `TableCell`.

### TableRow {#TableRow}
```
public static final int TableRow
```


Spécifie que le nœud est une `TableRow`.

### Title {#Title}
```
public static final int Title
```


Spécifie que le nœud est un `Title`.

