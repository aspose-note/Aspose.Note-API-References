---
title: "DisplayUnitsConverter"
second_title: "Référence d'API Aspose.Note pour Java"
description: "La classe contient les méthodes de conversion des valeurs."
type: docs
weight: 19
url: /fr/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

La classe contient les méthodes de conversion des valeurs.
## Méthodes

| Méthode | Description |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Convertit les pouces en points. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Convertit les millimètres en pouces. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Convertit les millimètres en points. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Convertit les pixels en points à la résolution de pixel spécifiée. |
| [pointToInch(float points)](#pointToInch-float-) | Convertit les points en pouces. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Convertit les points en pixels à la résolution de pixel spécifiée. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Convertit les pouces en points.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| pouces | float | La valeur à convertir en pouces. |

**Returns:**
float - Le `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Convertit les millimètres en pouces.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| mm | float | La valeur à convertir en millimètres. |

**Returns:**
float - Le `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Convertit les millimètres en points.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| mm | float | La valeur à convertir en millimètres. |

**Returns:**
float - Le `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Convertit les pixels en points à la résolution de pixel spécifiée.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| pixels | int | La valeur à convertir en pixels. |
| dpi | float | Résolution de l'écran. |

**Returns:**
float - Le `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Convertit les points en pouces.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| points | float | La valeur à convertir en points. |

**Returns:**
float - Le `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Convertit les points en pixels à la résolution de pixel spécifiée.

**Parameters:**
| Paramètre | Type | Description |
| --- | --- | --- |
| points | float | La valeur à convertir en points. |
| dpi | float | Résolution de l'écran. |

**Returns:**
int - Le `int`.
