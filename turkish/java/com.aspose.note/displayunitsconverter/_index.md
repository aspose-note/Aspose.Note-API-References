---
title: "DisplayUnitsConverter"
second_title: "Aspose.Note for Java API Referansı"
description: "Sınıf, değerleri dönüştürme yöntemlerini içerir."
type: docs
weight: 19
url: /tr/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

Sınıf, değerleri dönüştürme yöntemlerini içerir.
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | İnçleri puana dönüştürür. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Milimetreleri inçe dönüştürür. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Milimetreleri puana dönüştürür. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Belirtilen piksel çözünürlüğünde pikselleri puana dönüştürür. |
| [pointToInch(float points)](#pointToInch-float-) | Puanları inçe dönüştürür. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Belirtilen piksel çözünürlüğünde puanları piksellere dönüştürür. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


İnçleri puana dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| inç | float | İnç cinsinden dönüştürülecek değer. |

**Returns:**
float - Bu `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Milimetreleri inçe dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| mm | float | Milimetre cinsinden dönüştürülecek değer. |

**Returns:**
float - Bu `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Milimetreleri puana dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| mm | float | Milimetre cinsinden dönüştürülecek değer. |

**Returns:**
float - Bu `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Belirtilen piksel çözünürlüğünde pikselleri puana dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| piksel | int | Piksel cinsinden dönüştürülecek değer. |
| dpi | float | Ekran çözünürlüğü. |

**Returns:**
float - Bu `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Puanları inçe dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| puan | float | Puan cinsinden dönüştürülecek değer. |

**Returns:**
float - Bu `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Belirtilen piksel çözünürlüğünde puanları piksellere dönüştürür.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| puan | float | Puan cinsinden dönüştürülecek değer. |
| dpi | float | Ekran çözünürlüğü. |

**Returns:**
int - Bu `int`.
