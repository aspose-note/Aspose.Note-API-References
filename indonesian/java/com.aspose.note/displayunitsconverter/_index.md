---
title: "DisplayUnitsConverter"
second_title: "Referensi API Aspose.Note untuk Java"
description: "Kelas ini berisi metode untuk mengonversi nilai."
type: docs
weight: 19
url: /id/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

Kelas ini berisi metode untuk mengonversi nilai.
## Metode

| Metode | Deskripsi |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Mengonversi inci ke poin. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Mengonversi milimeter ke inci. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Mengonversi milimeter ke poin. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Mengonversi piksel ke poin pada resolusi piksel yang ditentukan. |
| [pointToInch(float points)](#pointToInch-float-) | Mengonversi poin ke inci. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Mengonversi poin ke piksel pada resolusi piksel yang ditentukan. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Mengonversi inci ke poin.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| inci | float | Nilai yang akan dikonversi dalam inci. |

**Returns:**
float - nilai `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Mengonversi milimeter ke inci.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| mm | float | Nilai yang akan dikonversi dalam milimeter. |

**Returns:**
float - nilai `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Mengonversi milimeter ke poin.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| mm | float | Nilai yang akan dikonversi dalam milimeter. |

**Returns:**
float - nilai `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Mengonversi piksel ke poin pada resolusi piksel yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| piksel | int | Nilai yang akan dikonversi dalam piksel. |
| dpi | float | Resolusi layar. |

**Returns:**
float - nilai `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Mengonversi poin ke inci.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| poin | float | Nilai yang akan dikonversi dalam poin. |

**Returns:**
float - nilai `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Mengonversi poin ke piksel pada resolusi piksel yang ditentukan.

**Parameters:**
| Parameter | Tipe | Deskripsi |
| --- | --- | --- |
| poin | float | Nilai yang akan dikonversi dalam poin. |
| dpi | float | Resolusi layar. |

**Returns:**
int - `int`.
