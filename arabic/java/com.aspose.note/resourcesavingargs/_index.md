---
title: "ResourceSavingArgs"
second_title: "مرجع Aspose.Note for Java API"
description: "يوفر بيانات لحدث ResourceSaving."
type: docs
weight: 80
url: /ar/java/com.aspose.note/resourcesavingargs/
---

**Inheritance:**
java.lang.Object
```
public class ResourceSavingArgs
```

يوفر بيانات لحدث ResourceSaving.
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getDocument()](#getDocument--) | يحصل على مستند الحفظ. |
| [getFileName()](#getFileName--) | يحصل على اسم الملف. |
| [getKeepStreamOpen()](#getKeepStreamOpen--) | يحصل أو يضبط قيمة تشير إلى ما إذا كان يجب إبقاء الدفق مفتوحًا. |
| [getStream()](#getStream--) | يحصل أو يضبط الدفق المستخدم لتخزين المورد. |
| [getUri()](#getUri--) | يحصل أو يضبط الـ uri للوصول إلى المورد المخزن. |
| [setKeepStreamOpen(boolean value)](#setKeepStreamOpen-boolean-) | يحصل أو يضبط قيمة تشير إلى ما إذا كان يجب إبقاء الدفق مفتوحًا. |
| [setStream(OutputStream value)](#setStream-java.io.OutputStream-) | يحصل أو يضبط الدفق المستخدم لتخزين المورد. |
| [setUri(String value)](#setUri-java.lang.String-) | يحصل أو يضبط الـ uri للوصول إلى المورد المخزن. |
### getDocument() {#getDocument--}
```
public final Document getDocument()
```


يحصل على مستند الحفظ.

**Returns:**
[Document](../../com.aspose.note/document)
### getFileName() {#getFileName--}
```
public final String getFileName()
```


يحصل على اسم الملف.

**Returns:**
java.lang.String
### getKeepStreamOpen() {#getKeepStreamOpen--}
```
public final boolean getKeepStreamOpen()
```


يحصل أو يضبط قيمة تشير إلى ما إذا كان يجب إبقاء الدفق مفتوحًا.

**Returns:**
boolean
### getStream() {#getStream--}
```
public final OutputStream getStream()
```


يحصل أو يضبط الدفق المستخدم لتخزين المورد.

**Returns:**
java.io.OutputStream
### getUri() {#getUri--}
```
public final String getUri()
```


يحصل أو يضبط الـ uri للوصول إلى المورد المخزن.

**Returns:**
java.lang.String
### setKeepStreamOpen(boolean value) {#setKeepStreamOpen-boolean-}
```
public final void setKeepStreamOpen(boolean value)
```


يحصل أو يضبط قيمة تشير إلى ما إذا كان يجب إبقاء الدفق مفتوحًا.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | boolean |  |

### setStream(OutputStream value) {#setStream-java.io.OutputStream-}
```
public final void setStream(OutputStream value)
```


يحصل أو يضبط الدفق المستخدم لتخزين المورد.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | java.io.OutputStream |  |

### setUri(String value) {#setUri-java.lang.String-}
```
public final void setUri(String value)
```


يحصل أو يضبط الـ uri للوصول إلى المورد المخزن.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | java.lang.String |  |

