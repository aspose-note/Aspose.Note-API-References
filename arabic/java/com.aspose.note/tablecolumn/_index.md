---
title: "TableColumn"
second_title: "مرجع Aspose.Note for Java API"
description: "يمثل عمود جدول."
type: docs
weight: 89
url: /ar/java/com.aspose.note/tablecolumn/
---

**Inheritance:**
java.lang.Object
```
public final class TableColumn
```

يمثل عمود جدول.
## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [TableColumn()](#TableColumn--) |  |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getLockedWidth()](#getLockedWidth--) | يحصل على قيمة تشير إلى ما إذا كان عمود الجدول يمتلك عرضًا مقفلًا ولا يتم تغيير حجمه تلقائيًا ليتناسب مع محتوى الجدول. |
| [getWidth()](#getWidth--) | يحصل على العرض. |
| [setLockedWidth(boolean value)](#setLockedWidth-boolean-) | يعيّن قيمة تشير إلى ما إذا كان عمود الجدول يمتلك عرضًا مقفلًا ولا يتم تغيير حجمه تلقائيًا ليتناسب مع محتوى الجدول. |
| [setWidth(float value)](#setWidth-float-) | يضبط العرض. |
### TableColumn() {#TableColumn--}
```
public TableColumn()
```


### getLockedWidth() {#getLockedWidth--}
```
public boolean getLockedWidth()
```


يحصل على قيمة تشير إلى ما إذا كان عمود الجدول يمتلك عرضًا مقفلًا ولا يتم تغيير حجمه تلقائيًا ليتناسب مع محتوى الجدول. بشكل افتراضي، عرض العمود غير مقفل.

**Returns:**
boolean
### getWidth() {#getWidth--}
```
public float getWidth()
```


يحصل على العرض.

**Returns:**
float
### setLockedWidth(boolean value) {#setLockedWidth-boolean-}
```
public void setLockedWidth(boolean value)
```


يعيّن قيمة تشير إلى ما إذا كان عمود الجدول يمتلك عرضًا مقفلًا ولا يتم تغيير حجمه تلقائيًا ليتناسب مع محتوى الجدول. بشكل افتراضي، عرض العمود غير مقفل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | boolean |  |

### setWidth(float value) {#setWidth-float-}
```
public void setWidth(float value)
```


يضبط العرض.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float |  |

