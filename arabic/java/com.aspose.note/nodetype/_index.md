---
title: "NodeType"
second_title: "مرجع Aspose.Note for Java API"
description: "يحدد نوع العقدة."
type: docs
weight: 52
url: /ar/java/com.aspose.note/nodetype/
---

**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class NodeType extends System.Enum
```

يحدد نوع العقدة.
## الحقول

| حقل | الوصف |
| --- | --- |
| [AttachedFile](#AttachedFile) | يحدد أن العقدة هي `AttachedFile`. |
| [Document](#Document) | يحدد أن العقدة هي `Document`. |
| [Image](#Image) | يحدد أن العقدة هي `Image`. |
| [InkDrawing](#InkDrawing) | يحدد أن العقدة هي [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing). |
| [InkParagraph](#InkParagraph) | يحدد أن العقدة هي [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph). |
| [InkWord](#InkWord) | يحدد أن العقدة هي [InkWord](../../com.aspose.note/nodetype\#InkWord). |
| [Loop](#Loop) | يحدد أن العقدة هي [Loop](../../com.aspose.note/nodetype\#Loop). |
| [Outline](#Outline) | يحدد أن العقدة هي `Outline`. |
| [OutlineElement](#OutlineElement) | يحدد أن العقدة هي `OutlineElement`. |
| [OutlineGroup](#OutlineGroup) | يحدد أن العقدة هي `OutlineGroup`. |
| [Page](#Page) | يحدد أن العقدة هي `Page`. |
| [PageSeries](#PageSeries) | يحدد أن العقدة هي `PageSeries`. |
| [RichText](#RichText) | يحدد أن العقدة هي `RichText`. |
| [Section](#Section) | يحدد أن العقدة هي `Section`. |
| [Table](#Table) | يحدد أن العقدة هي `Table`. |
| [TableCell](#TableCell) | يحدد أن العقدة هي `TableCell`. |
| [TableRow](#TableRow) | يحدد أن العقدة هي `TableRow`. |
| [Title](#Title) | يحدد أن العقدة هي `Title`. |
### AttachedFile {#AttachedFile}
```
public static final int AttachedFile
```


يحدد أن العقدة هي `AttachedFile`.

### Document {#Document}
```
public static final int Document
```


يحدد أن العقدة هي `Document`.

### Image {#Image}
```
public static final int Image
```


يحدد أن العقدة هي `Image`.

### InkDrawing {#InkDrawing}
```
public static final int InkDrawing
```


يحدد أن العقدة هي [InkDrawing](../../com.aspose.note/nodetype\#InkDrawing).

### InkParagraph {#InkParagraph}
```
public static final int InkParagraph
```


يحدد أن العقدة هي [InkParagraph](../../com.aspose.note/nodetype\#InkParagraph).

### InkWord {#InkWord}
```
public static final int InkWord
```


يحدد أن العقدة هي [InkWord](../../com.aspose.note/nodetype\#InkWord).

### Loop {#Loop}
```
public static final int Loop
```


يحدد أن العقدة هي [Loop](../../com.aspose.note/nodetype\#Loop).

### Outline {#Outline}
```
public static final int Outline
```


يحدد أن العقدة هي `Outline`.

### OutlineElement {#OutlineElement}
```
public static final int OutlineElement
```


يحدد أن العقدة هي `OutlineElement`.

### OutlineGroup {#OutlineGroup}
```
public static final int OutlineGroup
```


يحدد أن العقدة هي `OutlineGroup`.

### Page {#Page}
```
public static final int Page
```


يحدد أن العقدة هي `Page`.

### PageSeries {#PageSeries}
```
public static final int PageSeries
```


يحدد أن العقدة هي `PageSeries`.

### RichText {#RichText}
```
public static final int RichText
```


يحدد أن العقدة هي `RichText`.

### Section {#Section}
```
public static final int Section
```


يحدد أن العقدة هي `Section`.

### Table {#Table}
```
public static final int Table
```


يحدد أن العقدة هي `Table`.

### TableCell {#TableCell}
```
public static final int TableCell
```


يحدد أن العقدة هي `TableCell`.

### TableRow {#TableRow}
```
public static final int TableRow
```


يحدد أن العقدة هي `TableRow`.

### Title {#Title}
```
public static final int Title
```


يحدد أن العقدة هي `Title`.

