---
title: "DisplayUnitsConverter"
second_title: "Aspose.Note für Java API-Referenz"
description: "Die Klasse enthält die Methoden zum Konvertieren von Werten."
type: docs
weight: 19
url: /de/java/com.aspose.note/displayunitsconverter/
---

**Inheritance:**
java.lang.Object
```
public final class DisplayUnitsConverter
```

Die Klasse enthält die Methoden zum Konvertieren von Werten.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [inchToPoint(float inches)](#inchToPoint-float-) | Konvertiert Zoll in Punkte. |
| [millimeterToInch(float mm)](#millimeterToInch-float-) | Konvertiert Millimeter in Zoll. |
| [millimeterToPoint(float mm)](#millimeterToPoint-float-) | Konvertiert Millimeter in Punkte. |
| [pixelToPoint(int pixels, float dpi)](#pixelToPoint-int-float-) | Konvertiert Pixel in Punkte bei der angegebenen Pixelauflösung. |
| [pointToInch(float points)](#pointToInch-float-) | Konvertiert Punkte in Zoll. |
| [pointToPixel(float points, float dpi)](#pointToPixel-float-float-) | Konvertiert Punkte in Pixel bei der angegebenen Pixelauflösung. |
### inchToPoint(float inches) {#inchToPoint-float-}
```
public static float inchToPoint(float inches)
```


Konvertiert Zoll in Punkte.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Zoll | float | Der Wert, der in Zoll umgewandelt werden soll. |

**Returns:**
float - Der `float`.
### millimeterToInch(float mm) {#millimeterToInch-float-}
```
public static float millimeterToInch(float mm)
```


Konvertiert Millimeter in Zoll.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| mm | float | Der Wert, der in Millimeter umgewandelt werden soll. |

**Returns:**
float - Der `float`.
### millimeterToPoint(float mm) {#millimeterToPoint-float-}
```
public static float millimeterToPoint(float mm)
```


Konvertiert Millimeter in Punkte.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| mm | float | Der Wert, der in Millimeter umgewandelt werden soll. |

**Returns:**
float - Der `float`.
### pixelToPoint(int pixels, float dpi) {#pixelToPoint-int-float-}
```
public static float pixelToPoint(int pixels, float dpi)
```


Konvertiert Pixel in Punkte bei der angegebenen Pixelauflösung.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Pixel | int | Der Wert, der in Pixel umgewandelt werden soll. |
| dpi | float | Bildschirmauflösung. |

**Returns:**
float - Der `int`.
### pointToInch(float points) {#pointToInch-float-}
```
public static float pointToInch(float points)
```


Konvertiert Punkte in Zoll.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Punkte | float | Der Wert, der in Punkte umgewandelt werden soll. |

**Returns:**
float - Der `float`.
### pointToPixel(float points, float dpi) {#pointToPixel-float-float-}
```
public static int pointToPixel(float points, float dpi)
```


Konvertiert Punkte in Pixel bei der angegebenen Pixelauflösung.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Punkte | float | Der Wert, der in Punkte umgewandelt werden soll. |
| dpi | float | Bildschirmauflösung. |

**Returns:**
int - Der `int`.
