---
title: "ResourceSavingArgs"
second_title: "Aspose.Note für Java API-Referenz"
description: "Stellt Daten für das ResourceSaving-Ereignis bereit."
type: docs
weight: 80
url: /de/java/com.aspose.note/resourcesavingargs/
---

**Inheritance:**
java.lang.Object
```
public class ResourceSavingArgs
```

Stellt Daten für das ResourceSaving-Ereignis bereit.
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getDocument()](#getDocument--) | Ruft das zu speichernde Dokument ab. |
| [getFileName()](#getFileName--) | Liest den Dateinamen. |
| [getKeepStreamOpen()](#getKeepStreamOpen--) | Ruft einen Wert ab oder legt ihn fest, der angibt, ob der Stream offen gehalten wird. |
| [getStream()](#getStream--) | Ruft den Stream ab oder legt ihn fest, der zum Speichern der Ressource verwendet wird. |
| [getUri()](#getUri--) | Ruft die URI ab oder legt sie fest, um auf die gespeicherte Ressource zuzugreifen. |
| [setKeepStreamOpen(boolean value)](#setKeepStreamOpen-boolean-) | Ruft einen Wert ab oder legt ihn fest, der angibt, ob der Stream offen gehalten wird. |
| [setStream(OutputStream value)](#setStream-java.io.OutputStream-) | Ruft den Stream ab oder legt ihn fest, der zum Speichern der Ressource verwendet wird. |
| [setUri(String value)](#setUri-java.lang.String-) | Ruft die URI ab oder legt sie fest, um auf die gespeicherte Ressource zuzugreifen. |
### getDocument() {#getDocument--}
```
public final Document getDocument()
```


Ruft das zu speichernde Dokument ab.

**Returns:**
[Document](../../com.aspose.note/document)
### getFileName() {#getFileName--}
```
public final String getFileName()
```


Liest den Dateinamen.

**Returns:**
java.lang.String
### getKeepStreamOpen() {#getKeepStreamOpen--}
```
public final boolean getKeepStreamOpen()
```


Ruft einen Wert ab oder legt ihn fest, der angibt, ob der Stream offen gehalten wird.

**Returns:**
boolean
### getStream() {#getStream--}
```
public final OutputStream getStream()
```


Ruft den Stream ab oder legt ihn fest, der zum Speichern der Ressource verwendet wird.

**Returns:**
java.io.OutputStream
### getUri() {#getUri--}
```
public final String getUri()
```


Ruft die URI ab oder legt sie fest, um auf die gespeicherte Ressource zuzugreifen.

**Returns:**
java.lang.String
### setKeepStreamOpen(boolean value) {#setKeepStreamOpen-boolean-}
```
public final void setKeepStreamOpen(boolean value)
```


Ruft einen Wert ab oder legt ihn fest, der angibt, ob der Stream offen gehalten wird.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean |  |

### setStream(OutputStream value) {#setStream-java.io.OutputStream-}
```
public final void setStream(OutputStream value)
```


Ruft den Stream ab oder legt ihn fest, der zum Speichern der Ressource verwendet wird.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.io.OutputStream |  |

### setUri(String value) {#setUri-java.lang.String-}
```
public final void setUri(String value)
```


Ruft die URI ab oder legt sie fest, um auf die gespeicherte Ressource zuzugreifen.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | java.lang.String |  |

